# Prisma CLI reads this file (it does not read the repo-root .env).
# Point this at your Postgres (the compose db service published on localhost).
DATABASE_URL=postgresql://cops:change-me@localhost:5433/cops?schema=public
